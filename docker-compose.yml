version: '3.8'

services:
  api:
    build: .
    container_name: uidai_risk_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - DEBUG=False
    restart: unless-stopped
